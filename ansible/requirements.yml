# ================================================================
# File: ansible/requirements.yml
# Purpose:
#   Baseline Ansible Galaxy collections required by the homelab repo.
#
# Notes:
#   - Install with: ansible-galaxy collection install -r ansible/requirements.yml
#   - Collections are kept explicit so operators can audit dependencies.
# ================================================================
---
collections:
  - name: effectivelywild.technitium_dns
    version: ">=1.1.2,<10.0.0"
  - name: community.proxmox
  - name: community.general
  - name: ansible.posix
  - name: ansible.utils
  - name: community.crypto
  - name: community.sops
  - name: community.routeros
  - name: ansible.netcommon